version: '3.8'

services:
  bot:
    build: .
    env_file:
      - .env
    volumes:
      # Mount persistent data directory for channel settings
      - bot_data:/app/data
    restart: unless-stopped
    environment:
      - DATA_DIR=/app/data

volumes:
  bot_data:
